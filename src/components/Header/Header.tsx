import React from 'react';
import styles from './Header.module.scss';

const Header: React.FC = () => {
  return (
    <header className={styles.header}>
      {/* Logo, navigation, favorites, cart will be added here */}
    </header>
  );
};

export default Header;
